import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  container: {
    marginTop: "95px",
  },
  gridContainer: {
    display: "flex",
    justifyContent: "center",
  },
  searchFields: {
    marginTop: "10px",
    marginBottom: "10px",
    maxWidth: "900px",
  },
  searchButton: {
    display: "flex",
  },
}));
